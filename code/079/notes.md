# issues:
in both IV tables: The replication package appears to have a bug where weighted instrumental variables estimation isn't properly implemented - while weighted OLS works correctly, combining weights with IV regression produces incorrect coefficients.